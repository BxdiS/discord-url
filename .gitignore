# Рабочие файлы, создаются из *.example при первом запуске.
# В репозиторий попадают только шаблоны.
config.toml
.env
watchlist.txt

# Прокси-списки с реальными учётными данными — никогда не коммитить.
proxies.txt
proxies.txt.local
*.local

# Локальное состояние
state.json
history.jsonl

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
